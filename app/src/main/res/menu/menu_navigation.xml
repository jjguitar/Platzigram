<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/search_navigationbar"
        android:title="@string/search_navigationbar"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_search"
        ></item>
    <item
        android:id="@+id/home_navigationbar"
        android:title="@string/home_navigationbar"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_home"
        ></item>
    <item
        android:id="@+id/account_navigationbar"
        android:title="@string/account_navigationbar"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_account"
        ></item>
</menu>